import { ErrorAlertDirective } from './error-alert.directive';

describe('ErrorAlertDirective', () => {
  it('should create an instance', () => {
    const directive = new ErrorAlertDirective();
    expect(directive).toBeTruthy();
  });
});
